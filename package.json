{
  "name": "backend-api",
  "version": "1.0.0",
  "description": "Backend server for BookMart application built with MERN stack (MongoDB, Express, ReactJs, Node.js). This Server handles user authentication, book management, order processing and other API functionalities necessary.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "build": "npm install && npm install --prefix backend/frontend && npm run build --prefix backend/frontend"
  },
  "author": "Atmik Tumane",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
